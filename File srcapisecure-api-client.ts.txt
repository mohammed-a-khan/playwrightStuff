@Regression
@enabled:true
@testCaseId:483439
@description:Verify Asset Layout API with valid data
@dataProvider:dp-for-excel
@dataFile:resources/${environment.name}/testdata/Regression_testdata.xlsx
@dataProviderClass:com.abc.customdataproviderimpl.DataProvider
@sheet:testData
@key:TC_34940
Scenario: Verify Asset Layout API with valid input data
    When I send a request to the asset layout API with data '${args[0]}'
    Then the response status code should be 200
    And the response should be successful
    And the response should contain the field "status"
    And the response field "status" should be "success"

@Regression
@enabled:true
@testCaseId:483440
@description:Verify Asset Layout API with invalid deal ID
@dataProvider:dp-for-excel
@dataFile:resources/${environment.name}/testdata/Regression_testdata.xlsx
@dataProviderClass:com.abc.customdataproviderimpl.DataProvider
@sheet:testData
@key:TC_34941
Scenario: Verify Asset Layout API with invalid deal ID
    When I send a request to the asset layout API with data '${args[0]}'
    Then the response status code should be 400
    And the response should contain the field "error"

@Regression
@enabled:true
@testCaseId:483441
@description:Verify Asset Layout API with invalid report date
@dataProvider:dp-for-excel
@dataFile:resources/${environment.name}/testdata/Regression_testdata.xlsx
@dataProviderClass:com.abc.customdataproviderimpl.DataProvider
@sheet:testData
@key:TC_34942
Scenario: Verify Asset Layout API with invalid report date
    When I send a request to the asset layout API with data '${args[0]}'
    Then the response status code should be 400
    And the response field "error" should contain "Invalid date format"
