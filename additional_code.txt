// File: src/framework/utils/logger.ts

/**
 * Logger class for test logging
 */
export class Logger {
  private readonly context: string;
  private logs: string[] = [];

  constructor(context: string) {
    this.context = context;
  }

  info(message: string): void {
    const logMessage = `[INFO][${this.context}] ${message}`;
    console.log(logMessage);
    this.logs.push(logMessage);
  }

  debug(message: string): void {
    const logMessage = `[DEBUG][${this.context}] ${message}`;
    console.debug(logMessage);
    this.logs.push(logMessage);
  }

  warn(message: string): void {
    const logMessage = `[WARN][${this.context}] ${message}`;
    console.warn(logMessage);
    this.logs.push(logMessage);
  }

  error(message: string): void {
    const logMessage = `[ERROR][${this.context}] ${message}`;
    console.error(logMessage);
    this.logs.push(logMessage);
  }

  getLogs(): string[] {
    return this.logs;
  }
}
